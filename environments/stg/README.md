Terraform cnp template works only with "stg" environment keys instead of "aat".
While both environment directory and tfvars file is set to "stg" the actuall
environment key within tfvars is still set to "aat".
